# Local development only — integration tests use Testcontainers.
services:
  nats:
    image: nats:2.12.7
    container_name: nestjs_jetstream_nats
    ports:
      - '4222:4222'
      - '8222:8222'
    command: '--js --http_port 8222'
    restart: unless-stopped
